---
applyTo: "**"
---
# Python3 Guidelines
This project includes `python3` and `pip3` pre-installed via `mise`. They available on the `PATH`, along with the Python language extensions for Python development.
